[package]
name = "{{project_name}}"
version = "0.1.0"
edition = "2021"

[dependencies]
velox-core = { path = "{{velox_path}}/velox-core" }
velox-dom = { path = "{{velox_path}}/velox-dom" }
velox-style = { path = "{{velox_path}}/velox-style" }
velox-renderer = { path = "{{velox_path}}/velox-renderer" }
velox-sfc = { path = "{{velox_path}}/velox-sfc" }

[build-dependencies]
velox-sfc = { path = "{{velox_path}}/velox-sfc" }
