[package]
name = "velox-dom"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Virtual DOM and layout engine for the Velox UI framework"
authors = ["fahimaloy"]
repository = "https://github.com/fahimaloy/velox"
publish = false

[dependencies]
