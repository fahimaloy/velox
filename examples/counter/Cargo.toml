[package]
name = "velox-example-counter"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/fahimaloy/velox"
description = "Interactive counter example for the Velox UI framework"

[[bin]]
name = "counter"
path = "src/main.rs"

[dependencies]
velox-dom = { path = "../../velox-dom" }
velox-style = { path = "../../velox-style" }
velox-renderer = { path = "../../velox-renderer", features = ["skia-native"] }
velox-core = { path = "../../velox-core" }

[build-dependencies]
velox-cli = { path = "../../velox-cli" }
